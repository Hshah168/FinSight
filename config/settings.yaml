# FinSight Configuration

project_name: "FinSight - Financial Intelligence Platform"
version: "1.0.0"

# Data Settings
data:
  input_folder: "data/input"
  archive_folder: "data/archive"
  processed_folder: "data/processed"
  
  # Expected file names
  gl_transactions_file: "gl_transactions.csv"
  budget_file: "budget_data.csv"
  
  # Date column name in data
  date_column: "date"
  date_format: "%Y-%m-%d"
  

